import {Component} from 'react';

export class NavBar extends Component {

    render() { // react automatically calls this method when the component is rendered
        return <h2 class="hightlight">this is a custom component</h2>;
    }
}